version: "2"
sql:
  - engine: "postgresql"
    schema: "./db/schema.sql"
    queries: "./db/queries"
    gen:
      go:
        package: "models"
        out: "models"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: false